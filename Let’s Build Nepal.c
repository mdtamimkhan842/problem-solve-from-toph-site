#include <stdio.h>
#include <string.h>
int main ()
{
    int i, j, k, T, length;
    char S[500];

    scanf("%d", &T);

    for (i=1; i<=T; i++)
    {
        gets(S);


    for (i=0; S[i] != '\0'; i++)
    {
        if (!(S[i] == ' ' && S[i] != '0'))
        {
        }
    }
    printf("Maile Nepal banauna sakchhu!!\n");


    for (i=0; S[i] != '\0'; i++)
    {
        if (S[i] == ' ' && S[i] != '0')
        {
        }
    }

    printf("Hami sabai milera Nepal Banau hai!!\n");
    }

    return 0;
}
